name: cookbook-dev
channels:
  - conda-forge
dependencies:
  - jupyter-book
  - pip
  - wradlib
  - arm_pyart
  - cartopy
  - matplotlib
  - numpy
  - xarray
  - metpy
  # this is needed for baltrad compilation
  - compilers
  - make
  - gnuconfig
  - bash
  - libaec
  - coreutils
  - autoconf
  - automake
  - tar
  - libblas=*=*netlib
  - liblapacke=*=*netlib
  - pip:
      - sphinx-pythia-theme
